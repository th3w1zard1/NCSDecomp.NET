using BioWare.Common;

// Forwarding types to maintain backward compatibility
// These delegate to the new BioWare.Common implementations

namespace BioWare.Common
{
    // Compatibility types removed due to conflicts with main implementations
}
